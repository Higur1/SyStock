import { prisma } from "./libs/prisma"
import { FastifyInstance } from "fastify";
import { number, z } from 'zod'
export async function appRoutes(app: FastifyInstance) {
    /*

    //Rotas Produto
    app.post('/adicionarProduto', async(request) => {

    });
    app.get('/listarProdutos', async () => {
        
    });
    app.get('/buscarProduto/:nome', async(request) =>{
    
    });
    app.get('/buscarProduto/:id', async (request) => {
        
    });
    app.get('/listarProdutoPorCategoria/:categoria', async (request) => {
        
    });
    app.patch('/atualizarProduto/:id', async (request) => {
        
    });
    app.delete('/excluirProduto/:id', async (request) => {
        
    });

    //Rotas Categoria
    app.post('/adicionarCategoria', async (request) => {
        
    });
    app.get('/listarCategorias', async () => {
        
    });
    app.get('/buscarCategoria/:id', async (request) => {
        
    });
    app.patch('/atualizarCategoria/:id', async (request) => {
        
    });
    app.delete('/excluirCategoria/:id', async (request) => {
        
    });

    //Rotas Usuario
    app.post('/adicionarUsuario', async (request) => {
        
    });
    app.get('/listarUsuarios', async () => {
        
    });
    app.get('/buscarUsuario/:id', async (request) => {
        
    });
    app.get('/buscarUsuario/:nome', async(request) => {

    });
    app.patch('/atualizarUsuario/:id', async(request) => {

    });
    app.delete('/excluirUsuario/:id', async(request) => {

    });

    //Rotas Estoque
    app.get('/listarEstoque', async (request) => {
        
    });
    app.get('/buscarProdutoEstoque/:nome', async (request) => {
        
    });
    app.patch('/AtualizarProdutoEstoque/:id', async (request) => {
        
    }); */
}
